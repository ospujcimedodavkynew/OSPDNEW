// This line references Vite client types. It can cause errors if the project's
// TypeScript configuration cannot locate the type definitions.
// Commenting it out to resolve the build error. This is safe as the current
// codebase does not appear to use Vite-specific environment variables or features.
// /// <reference types="vite/client" />
